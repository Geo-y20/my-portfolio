# Portfolio Visual Audit

Generated by inspecting the four private GitHub repositories listed below (all accessible via the authenticated `gh` CLI, account `Geo-y20`, org `akhnatonTradingAndDistribution`). Each repo was shallow-cloned and independently researched: README/docs, full file tree, diagram/screenshot search, and source-code verification of the real architecture.

**Headline finding: none of the four repos contain a standalone diagram or screenshot file** (no `.svg`, `.png`, `.jpg`, `.drawio`, `.excalidraw`, no `assets/`/`media`/`screenshots` folder). All existing diagrams are **Mermaid or ASCII-art code blocks embedded in markdown docs**. That means Option 1 ("reuse an existing diagram file") does not apply to any of the four — every diagram will be Option 2 (rebuild the verified Mermaid/ASCII flow as a clean SVG) or Option 3 (reconstruct from code where no diagram exists at all).

**Second headline finding — several factual mismatches between the current portfolio copy and the verified repositories.** These are flagged per-project below and need a decision before I touch the on-page text (a diagram that's correct while the paragraph next to it is wrong would make things worse, not better).

---

## 1. ATR Policies GPT

- **Repository:** https://github.com/akhnatonTradingAndDistribution/atr-policies-gpt
- **Access:** Available (clone succeeded)
- **Default branch:** `main`
- **Mapped portfolio project:** ATR GPT / Enterprise AI Assistant
- **Detected stack:** Python, FastAPI, PostgreSQL + `pgvector` (hybrid vector/keyword semantic memory), Jinja2 + vanilla JS frontend (no SPA framework), Docker + nginx reverse proxy. No CI/CD found.
- **Main entry points:** `server_fastapi/main.py`, routes under `server_fastapi/routes/` (chat, admin, mobile, sheets, hr, image, system)
- **Existing diagrams:** 8 Mermaid diagrams in `doc/03-architecture.md` (architecture, components, sequence, data-flow, request lifecycle, auth flow, DI graph, module deps) + more in `doc/06-business-logic.md`, `doc/05-database-documentation.md` (ERD). No standalone image files.
- **Existing screenshots:** None found.
- **Safe assets that can be reused:** None as files (diagrams are markdown text, not binary). The verified Mermaid flow itself is safe, generic, and reusable as the basis for a redrawn SVG.
- **Architecture summary (verified flow):** Client (browser/mobile) → nginx (TLS) → FastAPI (4 uvicorn workers) → route layer → service layer (orchestrator, governance, memory) → **one of three AI backends depending on mode**: OCI GenAI Agent Runtime (Policy-document RAG mode), Anthropic Claude (Assistant mode), OpenAI REST API (image generation only) → PostgreSQL + pgvector for persistence and hybrid semantic-memory retrieval, optional Redis session cache.
- **Confidence level:** High
- **⚠️ Discrepancy vs. current portfolio copy:**
  - Current copy says **"Azure-based"** — not found anywhere in the repo. The verified cloud AI backend is **Oracle Cloud Infrastructure (OCI) GenAI**, not Azure.
  - Current copy says **"integrated with Microsoft Teams"** — no Teams SDK, bot framework, or webhook code exists anywhere in the repo. Not verified.
  - Current copy says **"multi-provider... OpenAI, Claude, and local Ollama"** — Claude is verified (real RAG/assistant use). OpenAI is verified but **only for image generation**, not chat/RAG. **Ollama is not found** (one incidental string in a cache file, not integration code). The real third provider is **OCI GenAI**, which isn't mentioned in the current copy at all.
  - Retrieval is **PostgreSQL + pgvector**, not a dedicated vector database — worth stating precisely rather than generically.
- **Missing information:** whether `langgraph` (named in `doc/02-technology-stack.md`) is actually imported anywhere — not independently confirmed in source.
- **Recommended portfolio visual:** Clean SVG reconstructing the verified Mermaid architecture — client → FastAPI → mode router → {OCI GenAI | Claude | OpenAI(image)} → Postgres/pgvector — once the text copy is reconciled with this flow (see questions below).
- **Source files supporting this:** `server_fastapi/main.py`, `services/ai/ai_router.py`, `services/policy_mode/oci_gateway.py`, `services/assistant_mode/anthropic_gateway.py`, `services/image/image_service.py`, `services/memory/memory_retriever.py`, `database.py`, `doc/03-architecture.md`

---

## 2. Enterprise Sales Intelligence (Text-to-SQL)

No repository was supplied for this project in this pass — the current portfolio copy for the Oracle Text-to-SQL platform is **not covered by any of the 4 repos inspected** and remains as previously written (unverified by this audit; not to be touched by this pass).

---

## 3. Forecast View

- **Repository:** https://github.com/akhnatonTradingAndDistribution/forecast-view
- **Access:** Available (clone succeeded)
- **Default branch:** `main`
- **Mapped portfolio project:** Best fit is **"Forecasting & Decision Intelligence"** (confirmed a real forecasting system, not a generic dashboard or unrelated tool) — but the technique described in current copy is wrong (see below).
- **Detected stack:** FastAPI (not Flask, despite README/docs saying Flask — code is authoritative), Gunicorn+Uvicorn, PostgreSQL (`forecast_data` + `forecast_results_production` DBs), JWT auth, Bootstrap 5 + Chart.js frontend, Docker.
- **Main entry points:** `app.py` (dev), `wsgi.py` (prod, gunicorn), `app/__init__.py` (FastAPI factory), `forecast_engine/cli.py` (pipeline)
- **Existing diagrams:** None (no Mermaid, no standalone files).
- **Existing screenshots:** None. Only image asset is `static/img/logo.jpg` — the client's brand logo, **not reusable** (identifies the client).
- **Safe assets that can be reused:** None found.
- **Architecture summary (verified flow):** Postgres (`forecast_data`) → SKU mapping audit → panel build/true-demand correction → per-SKU **custom Prophet-style Ridge regression** + **LightGBM**, with statsmodels ETS/Holt-Winters and Croston as fallback models, best model chosen by wMAPE → forecast + decision metrics/root-cause rules written to `forecast_results_production` → optional LLM explanation step (OpenAI-compatible API or OCI GenAI) → FastAPI dashboard renders actual vs. AI-forecast vs. manual-forecast with Chart.js.
- **Confidence level:** High
- **⚠️ Discrepancy vs. current portfolio copy:** Current copy says **"ARIMA and time-series decomposition"**. The repo explicitly does **not** use ARIMA or Facebook/Meta Prophet (despite the internal naming) — the real production model is an **in-house Prophet-style Ridge regression + LightGBM ensemble**, with **statsmodels ETS/Holt-Winters and Croston** as fallback/secondary methods.
- **Missing information:** none blocking — architecture is clear and well documented.
- **Recommended portfolio visual:** SVG reconstructing the verified pipeline (Postgres → per-SKU model selection → forecast + root-cause metrics → dashboard), captioned "Architecture reconstructed from the project implementation." Model names should say "custom regression + gradient-boosted ensemble" rather than ARIMA.
- **Source files supporting this:** `forecast_engine/README.md`, `forecast_engine/models/ets_model.py`, `forecast_engine/models/lightgbm_qty.py`, `forecast_engine/explain/client.py`, `app/__init__.py`, `UPGRADE_LOG.md`

---

## 4. Automation OCR

- **Repository:** https://github.com/akhnatonTradingAndDistribution/Automation-OCR
- **Access:** Available (clone succeeded)
- **Default branch:** `main`
- **Mapped portfolio project:** Currently folded into "OCR & Document Intelligence" (enterprise archive item). Verified scope is narrower than the current description.
- **Detected stack:** FastAPI, Google Cloud Vision API (`google.cloud.vision`), Pillow, Pydantic v2, Docker. Stateless — **no database, no persistent storage.**
- **Main entry points:** `main.py`, `app/main.py`, routes in `app/api/routes/upload.py`, `health.py`
- **Existing diagrams:** 6 Mermaid diagrams embedded in `docs/01-architecture.md`, `docs/06-data-model.md`, `docs/08-activity-diagrams.md` — generic, no business data, safe to redraw.
- **Existing screenshots:** None tracked in git.
- **Safe assets that can be reused:** None as files.
- **Architecture summary (verified flow):** Image upload (PNG/JPG/WEBP, ≤16MB) → preprocessing (RGB conversion, contrast/sharpness) → Google Cloud Vision `text_detection` with Arabic+English language hints → regex-based extraction of **batch/lot number, manufacturing date, expiry date (with future-date validation), and quantity** from **product label images** → structured JSON or rendered HTML result. No queue, no DB, no cloud storage — purely request/response.
- **Confidence level:** High
- **⚠️ Discrepancy vs. current portfolio copy:** Current copy says **"Bilingual OCR for finance cheque reading and supply-chain batch/expiry extraction."** Only the **second half is verified** — this repo does bilingual (Arabic/English) product-label batch/mfg/expiry/quantity extraction via Google Vision. **No cheque-reading code, model, or logic exists in this repo.** Either a separate undisclosed system handles cheques, or that claim should be dropped/rephrased to focus on label/document data extraction.
- **Missing information:** whether a separate cheque-OCR system exists elsewhere (not in scope of the 4 repos given).
- **Recommended portfolio visual:** SVG of the verified flow: label image → preprocessing → Google Vision OCR (AR/EN) → regex field extraction & validation → structured output. Copy should be narrowed to "product-label OCR" rather than "finance cheque reading" unless a cheque system is confirmed elsewhere.
- **Source files supporting this:** `app/ocr/engine.py`, `app/ocr/parser.py`, `app/ocr/hybrid.py`, `app/services/ocr_service.py`, `app/api/routes/upload.py`, `docs/01-architecture.md`

---

## 5. Text Digitalize System

- **Repository:** https://github.com/akhnatonTradingAndDistribution/Text-Digitlize-System
- **Access:** Available (clone succeeded)
- **Default branch:** `main`
- **Mapped portfolio project:** **No clean match in the current portfolio.** This is not classic OCR/scanning — it's a Flask/PostgreSQL editorial workflow for bilingual (Arabic/English) cosmetics product marketing copy (draft → regulatory review → final review → approved), plus a document-intelligence subsystem that ingests spec documents (PDF/DOCX/XLSX/PPTX/images) and auto-generates GSO (Gulf regulatory) -compliant bilingual layouts using an LLM.
- **Detected stack:** Flask 3, Flask-SQLAlchemy/Login/Migrate, PostgreSQL, PyMuPDF (`fitz`)/`pymupdf4llm` (primary parser) + PyPDF2 (legacy fallback), `python-docx`/`python-pptx`/`openpyxl`, LLM vision for embedded-image OCR, Anthropic Claude (current default AI provider), OCI GenAI (legacy provider), OpenAI (pricing/cost tracking present), ReportLab + `arabic-reshaper`/`python-bidi` for Arabic PDF export, `sentence-transformers` for optional semantic routing, Docker + Gunicorn.
- **Main entry points:** `wsgi.py`, `start.py`, `app/__init__.py::create_app`
- **Existing diagrams:** ASCII-art diagrams embedded in `docs/ARCHITECTURE.md`, `docs/AI_PIPELINE.md`, `docs/ACTIVITY_DIAGRAMS.md` (10 UML-style activity diagrams), `docs/DATABASE.md` (ERD) — all generic/safe.
- **Existing screenshots:** None useful — only a decorative illustration and site logo/brand assets (proprietary cosmetics-brand logos, not reusable).
- **Safe assets that can be reused:** None as files.
- **Architecture summary (verified flow):** Upload (PDF/DOCX/XLSX/PPTX/image) → validate/store (local disk, Postgres blob, or OCI Object Storage) → background parse (PyMuPDF text-layer extraction, or LLM vision for embedded images) → chunk + template/AI field extraction → LLM-assisted (Claude primary) bilingual GSO-compliant layout generation → compliance/quality gate → cached, served to chat UI, exportable to Excel/PDF. Separate CRUD workflow persists finalized product copy with a multi-role approval state machine.
- **Confidence level:** High
- **This is a genuinely different project from anything currently on the portfolio** — not OCR in the scanning sense, not the same as Automation-OCR, and not the same as ATR GPT despite also using Claude/OCI GenAI.
- **Missing information / open question for you:** should this become its own new portfolio entry ("Bilingual Product-Content & Regulatory Layout Platform" or similar), be folded into an existing project description, or left off the public portfolio entirely (it touches a multi-role internal approval workflow and regulatory compliance content, which may be more sensitive than the other projects)?
- **Recommended portfolio visual:** Hold — no diagram should be built until you confirm whether/how this project should appear.
- **Source files supporting this:** `docs/ARCHITECTURE.md`, `docs/AI_PIPELINE.md`, `app/utils/ai_service.py`, `app/services/document_intelligence/parsers/legacy_pdf_parser.py`, `app/services/products.py`

---

## 6. Social Listening

- **Repository:** https://github.com/akhnatonTradingAndDistribution/Social-Listening-
- **Access:** Available (clone succeeded)
- **Default branch:** `main`
- **Mapped portfolio project:** Promoted to its own featured case study, **Social Listening Intelligence Platform** (previously a one-line "Social Listening Intelligence" tile in Additional Work — that tile has been removed now that this is a full case study).
- **Detected stack:** Flask (wrapped by FastAPI/uvicorn as an ASGI shim via `WSGIMiddleware`), HuggingFace `transformers` + PyTorch (`AutoModelForSequenceClassification`), PostgreSQL (current production backend — most docs describe SQLite, but code is authoritative and is Postgres-only), Facebook Graph API + Instagram Graph API (official, token-based), Matplotlib/Seaborn + WeasyPrint/ReportLab for PDF reporting, Docker.
- **Main entry points:** `website-eva-all/app.py` (Flask), `website-eva-all/fastapi_app.py` (actual ASGI entrypoint run in Docker), `predict_and_tag_postgres.py` (production ML batch job), `Scraping/social_scraper.py` (unified FB/IG scraper)
- **Existing diagrams:** Mermaid diagrams embedded in `docs/01-architecture.md` (system architecture + module dependencies), `docs/06-data-model.md` (ERD), `docs/08-activity-diagrams.md` (6 flowcharts) — all generic, no client data, safe to redraw. No standalone diagram files.
- **Existing screenshots:** None — only brand logo/thumbnail images in `static/images/`, not reusable.
- **Safe assets that can be reused:** None as files (diagrams are markdown text).
- **Architecture summary (verified flow):** Facebook + Instagram Graph API → unified scraper → normalized into a per-brand, per-platform PostgreSQL schema → fine-tuned BERT sequence-classification model tags each comment (11 raw model classes mapped to a 9-category business taxonomy: FAQ, availability, delivery issues, pricing, competition, engagement, tone, filter, positive/negative; single tag per comment) → Flask routes read via a database adapter → dashboard, cross-brand analysis and PDF report export.
- **Confidence level:** High — verified directly against the scraper, ingestion scripts, ML tagging script, database adapter, migrations and Dockerfile, not just docs (and caught a real doc/code mismatch: docs describe SQLite, code is Postgres-only).
- **⚠️ Correction vs. the old placeholder copy:** The previous one-line description said "Classifies **Arabic Facebook** comments into sentiment and business-relevant categories." Verified scope is broader: **Facebook *and* Instagram** (not Facebook-only), across **8+ brands**, using a **fine-tuned BERT classifier** (not a generic sentiment model) mapped to a **9-category business taxonomy** (not just sentiment).
- **Missing information:** exact BERT base checkpoint (weights are gitignored, not in the repo).
- **Recommended portfolio visual:** SVG of the verified flow — implemented as `images/architecture/social-listening-architecture.svg`, wired into the new case-study page and the homepage's featured case-study grid.
- **Source files supporting this:** `Scraping/social_scraper.py`, `predict_and_tag_postgres.py`, `website-eva-all/smart_database_adapter.py`, `website-eva-all/app.py`, `website-eva-all/fastapi_app.py`, `website-eva-all/modules/comprehensive_service.py`, `migrations/full_all_brands/01_create_all_schemas_and_tables.sql`, `docs/01-architecture.md`

**Security note (same pattern as the other repos, not reproduced):** `Scraping/.env` and `website-eva-all/.env` are tracked in git with live Facebook/Instagram access tokens, a DB password and an SMTP password — flagged for human review/rotation, not acted on here.

---

## Non-portfolio security note

Unrelated to this visual-storytelling task, but worth a one-line flag since I was asked not to silently skip findings: three of the four repos (`atr-policies-gpt`, `forecast-view`, `Text-Digitlize-System`) have a **`.env` and/or a private key file (`.pem`) tracked in git**, and `Text-Digitlize-System/docs/DATABASE.md` contains what looks like a live Postgres connection string with credentials and an internal IP. No secret values are reproduced anywhere in this report or copied to any working directory. This is a repo-hygiene matter for you/the org to address separately — not something I acted on or need to fix as part of the portfolio update.

---

## Summary of decisions needed before implementation

1. **ATR GPT** — drop "Azure" and "Microsoft Teams" (unverified), replace the provider list with the verified one (OCI GenAI + Claude + OpenAI for images), and describe retrieval as Postgres/pgvector hybrid search? Or is there other context (a wrapper app, a different deployment) that justifies keeping Teams/Azure in the copy?
2. **Forecasting & Decision Intelligence** — replace "ARIMA" with the verified "custom Prophet-style Ridge regression + LightGBM ensemble (ETS/Croston fallback)"?
3. **OCR & Document Intelligence** — narrow the description to verified product-label OCR (batch/mfg/expiry/quantity via Google Vision), dropping "finance cheque reading" unless a separate system covers it?
4. **Text-Digitlize-System** — add as a new portfolio project, fold into an existing one, or leave off the public site?
